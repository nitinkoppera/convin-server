{
  "name": "convin-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development node server.js",
    "start": "NODE_ENV=production node server.js json-server --watch db.json",
    "watch": "json-server --watch db.json --port=8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.3"
  }
}
